'use strict';

/**
 * businesses-profile router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::businesses-profile.businesses-profile');
