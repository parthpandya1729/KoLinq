'use strict';

/**
 * businesses-profile service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::businesses-profile.businesses-profile');
